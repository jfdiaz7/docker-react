language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t jfdiaz/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jfdiaz/docker-react npm run test
  